# ボートレース3連単予測LINEボット 要件定義書

作成日: 2026-07-25 / 開発者: あおい（個人開発・ユーザー1名）

---

## 0. 前提と正直な見通し

- 舟券の**控除率は約25%**（払戻率75%）。何もしなければ長期回収率は75%に収束する。
- したがって本プロジェクトのゴールは「必ず勝つ」ではなく、**オッズ（＝市場の予想）より正確な確率を出し、市場が過小評価している買い目だけを抽出する**こと。
- 成功基準はバックテストでの**回収率100%超**であり、的中率ではない。ただしユーザー要望により的中率も併記する。
- 投票の自動執行は行わない（購入判断は人間）。

### 「的中率が一番高い」の解釈（合意事項）

固定の「1-2-3」ではなく、**レースごとの条件付き確率**で最も高い組み合わせを出す。
1枠に弱い選手、5枠に強い選手がいれば、当然そのレースの最頻値は 1-2-3 にならない。
→ 6艇それぞれの強さスコアからレース単位で120通りの確率を計算する（§4）。

---

## 1. スコープ

### やること
- LINEでレース場・レース番号を送ると、3連単の推奨買い目3〜5点＋根拠を返す
- 各買い目について：予測確率 / 現在オッズ / 期待値 を表示
- 「的中率トップ」と「期待値トップ」の両方を出す
- 過去データを蓄積し、バックテストで精度を検証する

### やらないこと（Phase 1時点）
- 自動投票、資金管理の自動化
- 複数ユーザー対応、課金
- 3連単以外の券種（2連単・拡連複など）

---

## 2. データソース（すべて無料）

| 用途 | 取得元 | 形式 | 更新 |
|---|---|---|---|
| 過去の出走表（番組表） | `https://www1.mbrace.or.jp/od2/B/dindex.html` | LZH→固定長テキスト（日次） | 前日 |
| 過去のレース結果 | `https://www1.mbrace.or.jp/od2/K/dindex.html` | LZH→固定長テキスト（日次） | 当日夜 |
| レーサー期別成績 | `boatrace.jp` ダウンロードページ | LZH（半期ごと） | 年2回 |
| 当日の出走表 | `boatrace.jp/owpc/pc/race/racelist?rno=&jcd=&hd=` | HTMLスクレイピング | 前日〜 |
| 直前情報（展示タイム・チルト・気象） | `.../race/beforeinfo?rno=&jcd=&hd=` | HTMLスクレイピング | 発走約40分前 |
| 3連単オッズ | `.../race/odds3t?rno=&jcd=&hd=` | HTMLスクレイピング | 随時 |
| レース結果 | `.../race/raceresult?rno=&jcd=&hd=` | HTMLスクレイピング | 発走後 |

**注意事項**
- 公式の「番組表閲覧」「オッズ情報・結果」「競走成績閲覧」サービスは2025年3月5日で終了済み。上記2つのダウンロードサイトは継続中。
- LZH解凍はCloudflare Workers上では困難 → **過去データの取り込みはGitHub Actions（Python）側で行い**、パース済みデータをD1に投入する。
- スクレイピングは1秒以上の間隔を空け、User-Agentを明示。公式サイトに負荷をかけない。
- `jcd` はレース場コード（01桐生 … 24大村）。

---

## 3. アーキテクチャ

```
┌─────────────── GitHub Actions（無料枠）───────────────┐
│  日次バッチ (Python)                                  │
│   ├ 番組表/成績LZHをDL → パース                        │
│   ├ 特徴量を計算                                       │
│   ├ D1へ書き込み（HTTP API経由）                        │
│   └ 週次: LightGBM学習 → モデルをJSON化してR2/D1へ       │
└──────────────────────────────────────────────────────┘
                          │
┌───────────── Cloudflare Workers（無料枠）─────────────┐
│  Hono / TypeScript                                    │
│   ├ POST /webhook  ← LINE Messaging API               │
│   │    ├ 署名検証（X-Line-Signature）                  │
│   │    ├ 入力パース（例:「大村 12」）                   │
│   │    ├ 直前情報＋オッズをフェッチ                     │
│   │    ├ 推論（Plackett-Luce → 120通り）               │
│   │    └ Reply API で返信（通数課金なし）               │
│   └ Cron Trigger: 当日開催レースの事前キャッシュ         │
│                                                       │
│  D1 (SQLite)  : races / entries / results / features  │
│  KV or R2     : モデルパラメータ、日次キャッシュ         │
└──────────────────────────────────────────────────────┘
```

### 無料枠の確認
| サービス | 無料枠 | 本件の想定使用量 |
|---|---|---|
| Cloudflare Workers | 100,000 req/日 | 1日数十 req |
| Cloudflare D1 | 5GB / 500万行読取per日 | 数百MB |
| Cloudflare Cron Triggers | 無料 | 1日数回 |
| GitHub Actions | 2,000分/月（publicは無制限） | 月100分程度 |
| LINE Messaging API | 応答メッセージは**通数無制限・無料** | Replyのみ使用 |

**重要**: LINEはPush（自動通知）だけが無料枠200通/月を消費する。こちらから話しかけて返してもらう形（Reply）なら完全無料。将来「締切5分前に自動通知」を足すなら、月200通の制約が効いてくる。

### Cloudflare Workersを選んだ制約
- Pythonの機械学習ライブラリは動かない → **学習はGitHub Actions、推論はWorkers上のTypeScript実装**に分離する。
- LightGBMのモデルは決定木の集合なので、JSON化してTypeScriptで木を辿る推論器を書けば再現できる（Phase 2で実装）。

---

## 4. 予測モデル

### Phase 1: 統計ルールベース（まずこれで動かす）

**ステップ1: 各艇の「強さスコア」sᵢ を算出**

線形結合＋指数化：

```
log(sᵢ) = Σ wₖ · xᵢₖ
```

特徴量 xᵢₖ の候補：

| カテゴリ | 特徴量 |
|---|---|
| 枠 | 進入コース、レース場別のコース別1着率（大村1コース≒60% / 戸田≒50%と場差が大きい） |
| 選手 | 全国勝率・2連対率、当地勝率・2連対率、級別(A1/A2/B1/B2)、年齢、体重 |
| 直近 | 今節成績、平均ST、F/L回数 |
| 機材 | モーター2連対率、ボート2連対率、部品交換履歴 |
| 直前 | 展示タイム（相対順位）、チルト角、周回展示の伸び |
| 環境 | 風向・風速、波高、気温・水温、安定板の有無 |

初期重み `w` は、過去2〜3年のデータで **多項ロジスティック回帰**を1回だけ回して決める（GitHub Actions側で実行、係数をJSONで配布）。

**ステップ2: Plackett-Luceモデルで120通りに展開**

1着だけでなく着順全体の確率を、スコアから整合的に導く：

```
P(i→j→k) = sᵢ/Σs · sⱼ/(Σs−sᵢ) · sₖ/(Σs−sᵢ−sⱼ)
```

120通りすべての確率が出て、合計が1になる。これが「レースごとの的中率」の定義になる。

**ステップ3: 期待値でフィルタ**

```
EV(組) = P(組) × オッズ(組)
```

- `EV > 1.0` の組を候補とする
- 候補が多すぎる場合は EV の高い順に上位5点
- 候補がゼロなら「このレースは見送り推奨」と返す（重要）

### Phase 2: 機械学習（データが貯まってから）

- LightGBM の LambdaRank（着順ランキング学習）または多クラス分類
- 出力は各艇の1着確率 → 同じくPlackett-Luceで展開
- 学習データ: 過去3〜5年、約8万レース想定
- Phase 1 と同じインターフェースにして差し替え可能にする

### キャリブレーション
予測確率が実測とズレていないか、信頼度ビン別に検証（reliability diagram）。ここが崩れていると期待値計算が全部無意味になるので必須。

---

## 5. LINE UX

### 入力
```
大村 12
24 12        ← 場コードでも可
おおむら12    ← 表記ゆれ吸収
```
- レース場名は漢字・ひらがな・カタカナ・場コードすべて受け付ける
- レース番号のみ送った場合は「どのレース場？」と聞き返す
- `今日` と送ると本日の開催場一覧を返す

### 出力（Flex Message）
```
🚤 大村 12R  締切 20:35

【期待値トップ】
① 1-3-2   確率 8.2% / 12.4倍 / EV 1.02
② 1-3-4   確率 4.1% / 28.7倍 / EV 1.18
③ 3-1-2   確率 3.4% / 35.0倍 / EV 1.19

【的中率トップ】
  1-2-3   確率 11.8% / 7.2倍 / EV 0.85 ⚠低EV

【根拠】
・1号艇 山田(A1) 当地勝率7.21・展示1位
・3号艇 モーター2連率48%（節間トップ）
・北風3m → まくり差しが決まりやすい条件
・1-2-3はオッズが売れすぎ（EV0.85）で非推奨

⚠ 予測であり的中を保証するものではありません
```

### エラー処理
- 発売締切後 → 結果を表示
- 開催なし → 「本日は開催がありません」
- 直前情報未公開 → 「展示前の暫定予想」と明示

---

## 6. データベース設計（D1 / SQLite）

```sql
races(race_id PK, date, jcd, rno, grade, deadline_at)
entries(race_id, lane, racer_id, racer_name, class,
        win_rate_national, win_rate_local, motor_2rate, boat_2rate,
        avg_st, ex_time, tilt, ...)
results(race_id, first, second, third, trifecta_payout, ...)
weather(race_id, wind_dir, wind_speed, wave, temp, water_temp, stabilizer)
predictions(race_id, combo, prob, odds_at_pred, ev, predicted_at)
model_params(version, json, created_at)
```

`predictions` を残すことで、**実運用での的中率・回収率を後から検証できる**。これがないと改善のしようがない。

---

## 7. 評価指標（バックテスト）

| 指標 | 目標 |
|---|---|
| 1着的中率（単勝相当） | 50%超（1号艇平均≒55%が基準線） |
| 3連単的中率（推奨5点） | 20%超 |
| **回収率（推奨5点・EV>1.0のみ）** | **100%超（最重要）** |
| Log Loss | ベースライン（人気順確率）を下回る |
| キャリブレーション誤差 | 予測10% → 実測8〜12%に収まる |

- 学習期間と検証期間は**必ず時系列で分割**（未来のデータで学習しない）
- 「レースを見送る」判断も含めて回収率を計算する

---

## 8. 開発フェーズ

| # | 内容 | 完了条件 |
|---|---|---|
| 1 | データ取得基盤 | 過去3年分がD1に入り、日次で自動更新される |
| 2 | 特徴量＋ロジスティック回帰 | 6艇の1着確率が出る |
| 3 | Plackett-Luce + 期待値計算 | 120通りの確率とEVが出る |
| 4 | バックテスト | 回収率・的中率がレポートされる |
| 5 | LINE Bot（Workers） | 「大村 12」で結果が返る |
| 6 | 運用・改善 | predictionsテーブルで実績追跡 |
| 7 | LightGBMへ差し替え | バックテストでPhase1を上回る |

---

## 9. 未決事項

- [ ] Phase 1のバックテスト対象期間（2年 / 3年 / 5年）
- [ ] 推奨点数の上限（固定5点 / EV閾値で可変）
- [ ] 締切前の自動Push通知を入れるか（LINE無料枠200通/月の制約あり）
- [ ] 資金管理（ケリー基準による賭け金提案）を出すか
- [ ] 予想の「根拠」文をルール生成にするかLLMで生成するか

---

## 参考

- [BOAT RACE 公式 ダウンロード・他](https://www.boatrace.jp/owpc/pc/extra/data/download.html)
- [競走成績ダウンロード](https://www1.mbrace.or.jp/od2/K/dindex.html)
- [番組表ダウンロード](https://www1.mbrace.or.jp/od2/B/dindex.html)
- [Messaging APIの料金 | LINE Developers](https://developers.line.biz/ja/docs/messaging-api/pricing/)
