# 運用マニュアル

仕組み・精度・セットアップ手順は [README.md](README.md) を参照してください。
このファイルは**動き出した後の運用**だけを扱います。

## 結論

**普段やることはありません。** LINEで予想を聞くだけです。

人間の判断が必要なのは「スクレイパーが壊れたとき」だけで、
それも異常を検知したらLINEに通知が来ます。

---

## 自動で動くもの

| いつ | 何が | 失敗したら |
|---|---|---|
| 毎朝 5:00 (JST) | 前日のレース結果を取り込み | 翌日の実行で自動的に追いつく |
| 30分おき | 全開催レースのオッズを保存 | その回のぶんが欠けるだけ |
| 毎日 12:30 | 本番のボットが正常か検証（モデルの新旧チェック込み） | LINEに通知が飛ぶ |
| 毎日 23:30 / 翌 5:00 | 予想の答え合わせ | 次回の実行で再試行 |
| 毎週月曜 6:00 | 双子で審査 → 採用なら「昨日まで学習」を配備 | LINEに通知が飛ぶ |
| worker/ に push したとき | テスト → 本番へ自動デプロイ | Actions が赤くなる |
| 毎月1日 | スケジュール維持のためのコミット | 警告メールが届く |

> かつてデプロイは「週次学習がモデルを採用したとき」だけで、
> モデルが採用されない週はコード修正が本番に届かない欠陥があった。
> 実際に数日分の改善が未反映のまま動いていたことがある（死活監視の
> モデルversionチェックはこの再発検知のためにある）。

### 再学習の採用基準

人間がPRを眺める代わりに、検証データ（審査用の双子が学習していない直近3か月）の
指標だけで機械的に判定します。新旧モデルとも**出荷された状態のまま**
（焼き込み済みの温度・段階較正）を同じ窓で測って比べます。
実際にデプロイされるアーティファクト同士の比較なので、モデル本体の
改善も較正の作り方の改善（val→out-of-fold化）も正しく反映されます。
較正は学習データ側（out-of-fold）だけで作られており、評価窓には触れません。

判定は3値です:

- **改善**（3連単NLLが0.002以上良化）→ 採用して配備
- **同等**（誤差の範囲）→ レシピは同じなので、学習データだけ
  昨日までに更新した配備を行う（ここで止めると+1週間のデータでは
  0.002も動かず、配備が永遠に古くなるため）
- **劣化**（NLLが0.005超悪化・的中率0.5pt超低下・1号艇固定以下）→ 配備しない

いずれの場合も Python/TypeScript のパリティテストが通ることが配備の条件で、
結果はLINEに届きます。

---

## 通知が来たらこうする（対応表）

LINEに届きうる通知の全パターンと、あなたがやることの一覧です。
**大半は「何もしない」が正解**です。

### 週次モデル再学習（毎週月曜の朝）

| 通知 | やること |
|---|---|
| ✅ 新しいモデルをデプロイしました | **何もしない。** 数字を眺めて喜ぶ |
| 🔄 レシピ同等のため、学習データだけ更新して配備 | **何もしない。** これが毎週の標準 |
| ― モデルを更新しませんでした（新モデルが劣化） | 1回なら**何もしない**（安全側に倒れただけ）。**2週連続**なら日次データ更新のログを確認。DBが欠けている兆候があれば「データベースのキャッシュが消えた」の手順でバックフィル |
| ⚠️ 週次モデル再学習が失敗しました | 通知内のログ末尾を読む。「レース数が少なすぎます」→ バックフィル手順へ。それ以外 → Actions の該当 run を開いてどのステップで落ちたか確認。一時的な障害なら Run workflow で再実行 |

### 死活監視（毎日12:30）

| 通知の中身 | 原因の見当 | やること |
|---|---|---|
| オッズを取得できていません: jcd=X, jcd=Y（**2場以上**） | オッズページの構造変化 | 下の「スクレイパー修理の手順」 |
| 出走表がN艇しか取れていません / 勝率が取れていない | 出走表ページの構造変化 | 同上（直すのは parseRacelist 側） |
| 1着確率の合計が1にならない / 不自然です | モデルか特徴量の壊れ | 「モデルを1つ前に戻したい」の手順でロールバック → 直近の変更を調査 |
| 本番のモデルが古いです（デプロイ漏れ） | 自動デプロイの失敗 | Actions →「Workerデプロイ」→ Run workflow。またはローカルで `cd worker && npm run deploy` |
| 接続できません / HTTP 5xx | Cloudflare か回線の一時障害 | 30分待って死活監視を手動 Run。続くなら `npx wrangler tail` でログを見る |

**スクレイパー修理の手順**（唯一、人間にしか直せない作業）:

```bash
cd worker
node scripts/fetch-fixtures.mjs 22 1 <今日YYYYMMDD>   # 最新HTMLを取得
npm test                # どのパーサがどう壊れたか失敗メッセージで特定
# src/scrape.ts の該当パーサを新しいHTMLに合わせて修正
npm test && git add -A && git commit -m "fix: ..." && git push   # pushで自動デプロイ
```

### λ実測（毎週水曜の昼）

| 通知 | やること |
|---|---|
| ― λ実測はまだ早い | **何もしない** |
| 📈 結果:「方向はプラスだが雑音と区別できない」 | **何もしない。** 来週を待つ |
| 📈 結果:「統計的に有意」かつ β > 0 | 待望のシグナル。**2週連続で有意**になったら、表示された λ\* を `worker/src/selection.ts` の `modelWeight`（現在0.25）に書き換えて push。1回だけなら様子見 |
| 📈 結果:「市場に足せていません」が数千レース規模で継続 | それが結論。モデル側の弾は撃ち尽くしているので、受け入れる（ボットは「市場とほぼ同等の予想＋見送り判定器」として使う） |
| 📈 回収率が有意な黒字（ベット100点以上で信頼区間が0より上） | おめでとうございます。ただし件数が数百点になるまでは賭け金を上げない |
| ⚠️ λ実測が失敗しました | Actions の run を確認。odds-db 成果物は**90日で消える**ので、長期停止後は貯め直しが必要 |

### 通知が来ない・その他

| 状況 | やること |
|---|---|
| 月曜・水曜に何も届かない | Actions タブを直接確認。workflows が止まっていれば再有効化。動いているのに届かないなら LINE トークン切れ →「LINEのトークンを再発行したとき」へ |
| GitHubから「Scheduled workflows disabled」メール | Actions タブで各 workflow を Enable（60日ルール） |
| ボットがLINEで返信しなくなった | `/health` を開いて生死確認 → 死んでいれば `npm run deploy`。生きていれば LINE Developers で Webhook 設定を確認 |

---

## 初回だけ必要な設定

自動化を全部効かせるには、GitHub に以下を登録してください。
**未設定でも壊れません**（該当する機能だけスキップされます）。

### Settings → Secrets and variables → Actions → Secrets

| 名前 | 用途 | 取得方法 |
|---|---|---|
| `CLOUDFLARE_API_TOKEN` | 自動デプロイ | Cloudflare ダッシュボード → My Profile → API Tokens → "Edit Cloudflare Workers" テンプレート |
| `CLOUDFLARE_ACCOUNT_ID` | 同上 | Workers の画面右側に表示されている |
| `LINE_CHANNEL_ACCESS_TOKEN` | 異常・結果の通知 | `.dev.vars` と同じ値 |
| `LINE_USER_ID` | 同上 | LINEでボットに「whoami」と送ると分かる |

任意（推奨）: Cloudflare 側にも `ADMIN_KEY` を登録すると、
成績ページ（/stats）と答え合わせの起動（/settle）が鍵なしで叩けなくなります。
`.dev.vars` に8文字以上のランダム文字列を書いて `npm run secrets` を実行し、
以後は `/stats?key=<その値>` の形で使います。

### Settings → Secrets and variables → Actions → Variables

| 名前 | 値 |
|---|---|
| `WORKER_URL` | `https://kyoutei-bot.<subdomain>.workers.dev` |

### Settings → Actions → General → Workflow permissions

- **Read and write permissions** を選択

> プッシュ通知は無料枠 月200通 を消費します（応答メッセージは無制限）。
> 通知が飛ぶのは異常時と週1回の学習結果だけなので、まず超えません。

---

## 自動化できないもの

### スクレイパーの修復

boatrace.jp のページ構造が変わると壊れます。**これだけは人間が直すしかありません。**

保存済みHTMLを使う `npm test` では検知できない（テストは通り続ける）ため、
毎日本番のボットに実際にリクエストを投げて確認しています。
異常があればLINEに通知が飛びます。

```bash
cd worker
node scripts/fetch-fixtures.mjs 22 1 20260720   # 最新HTMLを取り直す
npm test                                        # どこが壊れたか確認
# 差分を見ながら src/scrape.ts を直す
npm run deploy
```

### 60日ルール（自動化しているが保証はない）

GitHub は60日間リポジトリに動きがないとスケジュール実行を無効化します。
毎月1日に自動コミットして回避していますが、**これは「コミットがあれば活動中と
みなされる」という挙動に依存しており、確実ではありません。**

無効化される前に GitHub から警告メールが届きます。
届いたら Actions タブから再有効化してください。

---

## 壊れたときの対処

### データベースのキャッシュが消えた

日次データ更新が「レース数が少なすぎます」で止まったら、キャッシュ失効です。
Actions からバックフィルし直してください。

```
start=2023-07-01  end=2024-06-30
start=2024-07-01  end=2025-06-30
start=2025-07-01  end=(今日)
```

直近90日以内なら、Actions の成果物 `kyotei-db-backup` からも復旧できます。

### LINE のトークンを再発行したとき

```bash
cd worker
# .dev.vars を新しい値に書き換えてから
npm run secrets
```

GitHub Secrets の `LINE_CHANNEL_ACCESS_TOKEN` も忘れずに更新してください。
再デプロイは不要です。

### モデルを1つ前に戻したい

```bash
git log --oneline -- worker/src/model.json
git checkout <コミット> -- worker/src/model.json
cd worker && npm run deploy
```

---

## 次にやると良いこと

1. **λ（市場ブレンド率）の実測**（最優先、というより実質これしか残っていない）

   **Actions → 「λ実測」 → Run workflow** を押すだけです（結果はLINEに届く）。
   毎週水曜12:00にも自動実行され、データが足りないうちは「まだ早い」と届きます。

   手元で回す場合は2つ準備が要ります（Actions側なら不要）:
   ```bash
   # (1) ローカルの kyotei.db に直近の結果を取り込む
   cd pipeline
   python download.py --start 2026-07-20 --end <昨日> --workers 4
   python build_db.py --start 2026-07-20 --end <昨日>
   # (2) Actions →「オッズ収集」→ 成果物 odds-db を data/odds.db に展開してから
   python fit_lambda.py
   ```

   Benter式の α・β の実測値と、**本番の買い方の回収率（信頼区間つき）**が出ます。
   β が 0 に張り付いたら、モデルは市場に何も足せていないということです。
   既定で審査用の双子（pipeline/model_eval.json）を使い、モデルの学習期間と
   重なるレースは自動で除外されます（配備用モデルで測るとインサンプルになるため）。
   初回の手応えは有効500レース（数日）、判断に足るのは数千レース。
   累計は「オッズ収集」ワークフローのログで確認できます。
2. ~~LightGBM への差し替え~~ — 済み（3連単NLL 3.858 → 3.853、対称測定）
3. ~~2・3着の枠ペア較正~~ — 済み（3.853 → 3.790）
4. ~~当節成績・レース番号の特徴量~~ — 済み（3.790 → 3.776、市場との差を計4割圧縮）
5. ~~1着ステージの残差較正（firstブロック）~~ — 済み（3.771 → 3.766、市場差0.10切り）
6. 特徴量の追加探索・ハイパーパラメータ調整 — README の「試して駄目だったこと」を
   参照。風向・前づけ・進入・優勝戦フラグ・決まり手等はすべて実測済みで効かなかった
7. ~~out-of-fold較正~~ — 済み（-0.0076/-0.0043、2窓）。週次学習に組み込み済み
8. ~~モーター機歴の独自集計~~ — 実測して悪化（+0.007）。不採用（README参照）
9. ~~鮮度重み（半減期365日）~~ — 済み（-0.0029/-0.0020、2窓）。週次学習に組み込み済み
10. ~~検証窓の短縮（3ヶ月→1ヶ月）~~ — 双子方式で置き換え（審査は3ヶ月窓の
    まま、配備モデルだけ昨日まで学習）。1ヶ月窓はノイズ±0.003で判定不能だった

### 較正だけ貼り直したいとき

木はそのままで、2・3着の展開だけを直近データに合わせられます（数分）。

```bash
cd pipeline
python calibrate_stages.py --start <学習期間の翌日>
python export_fixture.py
cd ../worker && npm test && npm run deploy
```

前半で推定して後半で採点し、改善しなければ書き込みません。
