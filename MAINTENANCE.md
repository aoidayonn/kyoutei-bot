# 定期メンテナンス

自動で動き続けるものと、人間がやることの一覧。

## 自動で動くもの（放っておいてよい）

| いつ | 何が | 失敗したら |
|---|---|---|
| 毎朝 5:00 (JST) | 前日のレース結果を取り込み | 翌日の実行で自動的に追いつく |
| 30分おき | 全開催レースのオッズを保存 | その回のぶんが欠けるだけ |
| 毎日 23:30 / 翌 5:00 | 予想の答え合わせ | 次回の実行で再試行される |
| 毎週月曜 6:00 | 再学習して PR を作成 | 手動で Run workflow すればよい |

---

## 人間がやること

### 🔴 60日に1回はコミットする（最重要）

**GitHub は60日間コミットがないと、スケジュール実行を自動で無効化します。**

止まると以下が連鎖して壊れます。

1. 日次データ更新が止まる
2. 7日後、`data/kyotei.db` のキャッシュが失効して消える
3. 週次学習が「データがない」で失敗する
4. オッズ収集も止まり、貯めていたデータが伸びなくなる

無効化される前に GitHub からメールが届きます。届いたら Actions タブから
再有効化してください。何もコミットすることがなければ、README に1行足すだけで構いません。

### 🟡 週に1回：再学習 PR の確認とデプロイ

月曜に PR が作られます。本文にバックテストの数字が載っているので、
**前回より回収率が下がっていないか**を見てからマージしてください。
下がっていれば閉じて構いません（前のモデルのまま動き続けます）。

マージしただけでは反映されません。`model.json` は Worker にバンドルされているので
再デプロイが必要です。

```bash
git pull
cd worker
npm run deploy
```

### 🟡 Actions の失敗通知を見る

ワークフローが失敗すると GitHub からメールが届きます。
放置するとデータに穴が空きます。多くは再実行するだけで直ります。

---

## 壊れたときの対処

### スクレイパーが動かなくなった

boatrace.jp のマークアップが変わると壊れます。予想が返ってこない、
オッズが取れないといった症状が出ます。

```bash
cd worker
npm test                                        # どこが壊れたか確認
node scripts/fetch-fixtures.mjs 22 1 20260720   # 最新HTMLを取り直す
npm test                                        # 差分を見ながら src/scrape.ts を直す
```

### データベースのキャッシュが消えた

日次データ更新が「レース数が少なすぎます」で止まったら、キャッシュ失効です。
Actions からバックフィルし直してください。

```
start=2023-07-01  end=2024-06-30
start=2024-07-01  end=2025-06-30
start=2025-07-01  end=(今日)
```

直近90日以内なら、Actions の成果物 `kyotei-db-backup` からも復旧できます。

### LINE のトークンを再発行したとき

```bash
cd worker
# .dev.vars を新しい値に書き換えてから
npm run secrets
```

再デプロイは不要です。シークレットの更新は即座に反映されます。

---

## やらなくてよいこと

- **オッズ収集のために予想を出す必要はありません。** 全開催レースを自動で回っています
- **過去データの取得を急ぐ必要はありません。** 公式にアーカイブされているので後からいつでも取れます
- **Cloudflare / LINE の無料枠を気にする必要はありません。** 応答メッセージは通数無制限、Workers は10万リクエスト/日です

---

## 次にやると良いこと

1. **λ（市場ブレンド率）の実測** — オッズが3週間分（約1万レース）たまったら、
   `0.25` という暫定値が妥当か検証できます
2. **LightGBM への差し替え** — 線形モデルの表現力が今の頭打ちの原因です
3. **平均ST・F数の追加** — 出走表ページにはあるが未使用の特徴量
